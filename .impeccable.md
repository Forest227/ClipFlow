## Design Context

### Users

ClipFlow 的目标用户是在 macOS 上高频复制粘贴的人：开发者、设计师、内容创作者、研究人员和运营人员。他们在文档、浏览器、聊天工具、代码编辑器之间频繁切换，需要快速找回并复用复制过的文本、链接、代码片段和图片。他们的核心诉求是"复制→找回→再粘贴"这件高频小事变得更快、更自然、更安心。

**使用场景：** 用户通常在专注工作时使用 ClipFlow，通过 Option+V 在鼠标附近快速呼出面板，不打断当前上下文。他们期望工具像 Spotlight 一样轻量融入系统，而不是一个需要主动管理的"剪贴板数据库"。

**核心任务（Job to be Done）：** 在不离开当前工作上下文的情况下，用最少的操作找回并粘贴之前复制的内容。

### Brand Personality

**四词个性：** 流畅、高级、干净、可信

**语调：** 克制而专业，像一个可靠的系统级工具。文案简短直接，用中文表达时偏向口语化但不随意。状态反馈精准（"已从 Safari 捕获新内容"），避免过度拟人化或花哨措辞。

**情感目标：** 用户应感到 ClipFlow 是一个值得信赖的、贴合系统的高品质工具——它安静地做好自己的事，不制造干扰，也不让人担心隐私。

### Aesthetic Direction

**视觉基调：** 深色柔和分层表面 + 暖冷平衡的强调色。参考 Linear/Raycast 的现代开发者工具美学：精致、高效、信息密度适中。

**参考应用：**
- **Linear** — 精致的深色界面、清晰的信息层次、克制的色彩使用
- **Raycast** — 紧凑的操作面板、流畅的键盘交互、系统级融入感

**反参考（应避免的方向）：**
- 冷冰冰的后台管理系统（过于工程化、缺乏温度）
- 过度炫技的概念设计（牺牲清晰度和信息密度）
- 过于花哨或拟物化的界面（不符合 macOS 原生气质）

**主题模式：** 跟随系统外观设置，明暗双模式同等重要。深色模式已有完善的色板定义，浅色模式使用暖调柔和背景。

**色彩体系（已定义）：**
- 主强调色：蓝色系 `(0.30, 0.59, 0.92)` — 主操作按钮
- 内容类型色：橙色（快速粘贴）、青绿（智能分组）、浅蓝（代码/链接）、红色（隐私保护）
- 背景采用渐变分层 + 毛玻璃材质（`.ultraThinMaterial`）
- 深色模式：深蓝灰底 + 低透明度白色填充
- 浅色模式：暖白/淡蓝底 + 高透明度白色填充

**排版：**
- 标题：`.rounded` 设计字体，粗体
- 正文：系统默认字体，medium weight
- 代码内容：等宽字体 `.monospaced`
- 字号层级：30pt（主标题）→ 24pt（二级标题）→ 15pt（区块标题）→ 13pt（正文）→ 12pt（辅助）→ 11pt（时间戳）→ 9-10pt（标签/徽章）

**间距与圆角：**
- 面板圆角：24pt（快捷面板、浮层）→ 18pt（卡片、内容区）→ 16pt（输入框）→ 14pt（搜索框）→ 12pt（图标背景）→ 13pt（按钮）
- 内边距：14pt（面板内容区）→ 10-12pt（卡片、输入框）
- 元素间距：18pt（板块间）→ 12pt（组内间距）→ 8-10pt（行间距）→ 4-5pt（标签间距）
- 使用 `.continuous` 圆角风格（macOS 连续曲率）

**动效：**
- 交互弹簧：`interactiveSpring(response: 0.22, dampingFraction: 0.92)`
- 选中态：`easeOut(duration: 0.14)` + 微缩放 `0.995`
- 按压态：`easeOut(duration: 0.10)` + 微缩放 `0.985`
- 淡入淡出：`easeOut(duration: 0.16)`
- 浮层出现：缩放 `0.985` + 透明度组合过渡

### Design Principles

1. **指针优先，上下文不中断** — 最快的路径从鼠标附近开始。快捷面板贴近指针出现，操作完成后立即回到用户原来的工作上下文。永远不要让用户为了粘贴一个内容而切换太多窗口。

2. **识别优于回忆** — 用户不应依赖记忆来找内容。通过类型图标、来源应用、时间标签、颜色编码和预览缩略图，让每个条目一眼可识别。信息密度要高但不拥挤。

3. **隐私是主流程，不是设置项** — 敏感内容遮罩、排除应用、暂停监听、自动过期等隐私功能应在日常交互中可见、可理解、可操作，而不是深藏在设置页里。用户应随时清楚知道什么内容被保护了、处于什么保护状态。

4. **系统级融入 > 功能堆叠** — ClipFlow 应像 macOS 原生系统工具一样运作：状态栏、全局快捷键、毛玻璃材质、连续曲率圆角、辅助功能粘贴。宁可少一个功能，也不要破坏系统一致性。

5. **高密度 + 高可读 = 高效率** — 在紧凑的空间里展示尽可能多的有用信息。通过清晰的视觉层次（字重、字号、颜色对比）而不是大留白来实现可读性。每次呼出都应让用户在最短时间内找到目标。

### Tech Stack

- **SwiftUI** — 主要界面框架
- **AppKit (NSWindow, NSPanel, NSPopover, NSStatusItem)** — 状态栏、浮动面板、全局快捷键、窗口控制
- **Swift Package Manager** — 构建系统（纯 SPM，无 Xcode 项目文件）
- **macOS 14+ (Sonoma)** — 最低部署目标
- **Swift 6.0** — 严格并发安全
- **无第三方依赖** — 纯原生实现

### File Structure

```
Sources/ClipFlowApp/
├── ClipFlowApp.swift       — App 入口、窗口控制器、AppDelegate
├── ContentView.swift       — 主窗口完整 UI（Header、Sidebar、Library、Settings、所有子组件、调色板）
├── Models.swift            — 数据模型、分类器、Store（业务逻辑核心）
├── Services.swift          — 运行时服务（剪贴板监听、粘贴服务、快捷键、快捷面板控制器、HUD 视图）
├── StatusBarController.swift — 状态栏控制器、Popover 菜单视图、状态栏图标绘制
├── CloudSync.swift         — iCloud 文稿目录同步协调器
└── ClipFlowMotion.swift    — 动效参数常量
```
